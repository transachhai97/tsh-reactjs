import React from 'react';
import { ToastContainer } from 'react-toastify';

function Toastify() {
    return (
        <ToastContainer
            newestOnTop
        />
    );
}

export default Toastify;
